<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Polypole Filter Test Harness V10</title>
<style>
:root{--panel:rgba(15,23,42,.76);--cyan:#67e8f9;--text:#e2e8f0;--muted:#94a3b8;--border:rgba(103,232,249,.18);--amber:#fbbf24}*{box-sizing:border-box}body{margin:0;min-height:100vh;color:var(--text);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:radial-gradient(circle at top,#164e63 0%,#020617 46%,#000 100%)}.page{max-width:1210px;margin:0 auto;padding:18px}.header{display:flex;justify-content:space-between;align-items:flex-end;gap:16px;margin-bottom:16px;flex-wrap:wrap}h1{margin:0;font-size:32px;line-height:1.1;color:#cffafe;letter-spacing:-.03em}.subtitle{margin-top:7px;color:#cbd5e1;font-size:14px}.layout{display:grid;grid-template-columns:1fr 330px;gap:16px}.card{background:var(--panel);border:1px solid rgba(103,232,249,.12);border-radius:26px;box-shadow:0 24px 60px rgba(0,0,0,.36);backdrop-filter:blur(10px)}.main-card,.side-card,.panel{padding:16px}.lcd-wrap{background:#000;border:1px solid var(--border);border-radius:24px;padding:12px;box-shadow:inset 0 0 35px rgba(34,211,238,.08);margin-bottom:20px}#lcd{width:100%;height:260px;display:block;border-radius:18px}.knob-grid{display:grid;grid-template-columns:repeat(6,1fr);gap:18px 10px}.knob-stack{display:flex;flex-direction:column;align-items:center;gap:8px;min-width:118px}.knob-label{color:rgba(207,250,254,.84);font-size:11px;letter-spacing:.18em;text-align:center;white-space:nowrap}.mod-label{color:rgba(251,191,36,.92);font-size:9px;letter-spacing:.08em;text-align:center}.knob{position:relative;width:80px;height:80px;border-radius:50%;background:#020617;border:1px solid rgba(103,232,249,.22);box-shadow:inset 0 2px 18px rgba(0,0,0,.8);display:grid;place-items:center;touch-action:none;cursor:grab}.knob.small{width:46px;height:46px;border-color:rgba(251,191,36,.25);flex:0 0 auto}.knob:active{cursor:grabbing}.knob-face{position:absolute;width:64px;height:64px;border-radius:50%;background:linear-gradient(135deg,#475569,#020617);border:1px solid rgba(255,255,255,.12);box-shadow:0 10px 25px rgba(0,0,0,.45);transform:rotate(-135deg);transition:transform .045s linear}.small .knob-face{width:36px;height:36px;background:linear-gradient(135deg,#57534e,#020617)}.indicator{position:absolute;left:50%;top:4px;width:4px;height:25px;transform:translateX(-50%);border-radius:999px;background:var(--cyan);box-shadow:0 0 12px rgba(103,232,249,.9)}.small .indicator{height:15px;background:#fbbf24;box-shadow:0 0 10px rgba(251,191,36,.9)}.readout{min-height:21px;padding:3px 7px;border-radius:7px;background:rgba(0,0,0,.32);color:rgba(165,243,252,.95);font-family:ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,monospace;font-size:12px;text-align:center;white-space:nowrap}.mod-readout{color:#fde68a;font-size:10px}.mod-row{display:grid;grid-template-columns:34px 46px 34px;gap:4px;align-items:center;justify-content:center}.mini-select{width:34px;height:22px;background:#020617;color:#fde68a;border:1px solid rgba(251,191,36,.35);border-radius:6px;font-size:9px;padding:0}.mini-select.lfo{color:#a5f3fc;border-color:rgba(103,232,249,.35)}.controls{display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-top:16px}.env-panels{display:grid;grid-template-columns:1fr;gap:16px}.section-title{color:#fef3c7;font-weight:800;margin-bottom:10px;font-size:14px}.adsr-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.field{display:flex;flex-direction:column;gap:5px}.field label{font-size:11px;color:#cbd5e1;letter-spacing:.08em}.field input,.field select{width:100%;accent-color:#22d3ee}.value{font-family:ui-monospace,monospace;color:#a5f3fc;font-size:12px}.lfo-box{border:1px solid rgba(103,232,249,.12);border-radius:16px;padding:10px;margin-bottom:10px;background:rgba(2,6,23,.25)}.lfo-box:last-child{margin-bottom:0}.side-title-row{display:flex;justify-content:space-between;align-items:center;margin-bottom:12px;gap:10px}.side-title{color:#ccfbf1;font-size:14px;font-weight:800}.side-subtitle{color:var(--muted);font-size:12px;margin-top:3px}.led{width:12px;height:12px;border-radius:999px;background:#475569}.led.on{background:#34d399;box-shadow:0 0 13px #34d399}#spectrum{width:300px;height:300px;display:block;border-radius:18px;border:1px solid rgba(45,212,191,.2);box-shadow:0 22px 40px rgba(0,0,0,.36);max-width:100%}.module-tag{display:inline-block;margin-top:12px;padding:5px 8px;border-radius:999px;color:#bae6fd;background:rgba(14,165,233,.12);border:1px solid rgba(125,211,252,.16);font-size:11px;font-family:ui-monospace,monospace}.keyboard-card{margin-top:16px;padding:16px}.keyboard-help{font-size:12px;color:#cbd5e1;margin-bottom:12px}.keyboard{position:relative;height:170px;max-width:930px;user-select:none}.white-key{position:absolute;top:0;width:42px;height:170px;background:linear-gradient(#fff,#cbd5e1);border:1px solid #334155;border-radius:0 0 8px 8px;color:#0f172a;font-weight:800;font-size:11px;display:flex;align-items:flex-end;justify-content:center;padding-bottom:8px;cursor:pointer}.white-key.active{background:linear-gradient(#cffafe,#67e8f9)}.black-key{position:absolute;top:0;width:27px;height:105px;background:linear-gradient(#1f2937,#020617);border:1px solid #0f172a;border-radius:0 0 6px 6px;color:#f8fafc;z-index:2;font-size:10px;display:flex;align-items:flex-end;justify-content:center;padding-bottom:7px;cursor:pointer}.black-key.active{background:linear-gradient(#fbbf24,#78350f)}ul{margin:0;padding-left:20px;color:#cbd5e1;font-size:12px;line-height:1.65}@media(max-width:1000px){.layout{grid-template-columns:1fr}.controls{grid-template-columns:1fr}.knob-grid{grid-template-columns:repeat(3,1fr)}}@media(max-width:620px){.knob-grid{grid-template-columns:repeat(2,1fr)}.adsr-grid{grid-template-columns:repeat(2,1fr)}#lcd{height:220px}.keyboard{transform:scale(.72);transform-origin:left top;margin-bottom:-45px}}
</style>
</head>
<body>
<main class="page"><header class="header"><div><h1>Polypole Filter</h1><div class="subtitle">V10: fixed smooth MOD envelopes — no timer overlap, no jumps, clean idle after release.</div></div></header>
<section class="layout"><section class="card main-card"><div class="lcd-wrap"><canvas id="lcd" width="900" height="260"></canvas></div><div id="knobs" class="knob-grid"></div><div class="controls"><section class="card panel"><div class="section-title">Envelope Mod Sources</div><div class="env-panels"><div><div class="section-title">ENV2 MOD Envelope ADSR</div><div id="filterAdsr" class="adsr-grid"></div></div><div><div class="section-title">ENV3 MOD Envelope ADSR</div><div id="modAdsr" class="adsr-grid"></div></div></div></section><section class="card panel"><div class="section-title">Gated LFO Mod Sources</div><div id="lfoPanels"></div></section></div></section><aside class="side"><section class="card side-card"><div class="side-title-row"><div><div class="side-title">Test Module: Spectrum</div><div class="side-subtitle">300×300, log 20 Hz–20 kHz, 100 ms peak hold</div></div><div id="led" class="led"></div></div><canvas id="spectrum" width="300" height="300"></canvas><div class="module-tag">Keyboard → Amp ADSR → Polypole → Analyzer</div></section><section class="card side-card"><div class="section-title">Routing</div><ul><li><b>ENV1</b> is AMP only.</li><li><b>ENV2/ENV3</b> are smooth MOD envelopes only when selected and the small knob is turned left.</li><li><b>LFO1/2/3</b> reset/start on key down and stop after AMP release.</li><li>Small mod knob center = no modulation.</li></ul></section></aside></section>
<section class="card keyboard-card"><div class="section-title">ENV1 Amplitude ADSR + 3-Octave Keyboard</div><div id="ampAdsr" class="adsr-grid" style="max-width:520px;margin-bottom:14px"></div><div class="keyboard-help">Click keys or use computer keyboard. Notes trigger ENV1 amp, ENV2 MOD, ENV3 MOD, and gated LFOs.</div><div id="keyboard" class="keyboard"></div></section></main>
<script>
"use strict";
const clamp=(v,min,max)=>Math.min(max,Math.max(min,v)),lerp=(a,b,t)=>a+(b-a)*t,expMap=(x,min,max)=>min*Math.pow(max/min,x),invExpMap=(v,min,max)=>Math.log(v/min)/Math.log(max/min);
const liveMod={env:[0,0,0],lfo:[0,0,0]};
function makePeriodicSaw(ctx){const h=2048,real=new Float32Array(h+1),imag=new Float32Array(h+1);for(let n=1;n<=h;n++)imag[n]=-2/(Math.PI*n);return ctx.createPeriodicWave(real,imag,{disableNormalization:false})}
function warmCurve(amount){const n=8192,c=new Float32Array(n),drive=lerp(1,28,amount),bias=lerp(0,.18,amount),fold=lerp(0,.22,amount);for(let i=0;i<n;i++){let x=i/(n-1)*2-1,y=Math.tanh(drive*(x+bias*x*x))/(Math.tanh(drive));y=(1-fold)*y+fold*Math.sin(y*Math.PI*.78);c[i]=clamp(y,-.985,.985)}return c}
function poleFrequencies(cutoff,poles,spread){const out=[],base=Math.log2(clamp(cutoff,20,20000)),lo=Math.log2(40/1000),hi=Math.log2(16000/1000),wide=Math.pow(spread,1.25);for(let i=0;i<poles;i++){const t=poles===1?.5:i/(poles-1),compact=(t-.5)*spread*1.6,offset=lerp(compact,lerp(lo,hi,t),wide);out.push(clamp(Math.pow(2,base+offset),20,20000))}return out}
function stateWeights(s){s=clamp(s,0,1);if(s<=.5){const t=s/.5;return{lp:Math.cos(t*Math.PI*.5),bp:Math.sin(t*Math.PI*.5),hp:0,bpFocus:Math.sin(Math.PI*s)}}const t=(s-.5)/.5;return{lp:0,bp:Math.cos(t*Math.PI*.5),hp:Math.sin(t*Math.PI*.5),bpFocus:Math.sin(Math.PI*s)}}
function createPolypoleFilter(ctx){const input=ctx.createGain(),output=ctx.createGain(),pre=ctx.createGain(),sat=ctx.createWaveShaper(),tone=ctx.createBiquadFilter(),post=ctx.createGain(),wet=ctx.createGain(),lim=ctx.createDynamicsCompressor();tone.type="lowpass";tone.frequency.value=18000;tone.Q.value=.5;lim.threshold.value=-5;lim.knee.value=22;lim.ratio.value=14;lim.attack.value=.0015;lim.release.value=.08;input.connect(pre);pre.connect(sat);sat.connect(tone);tone.connect(post);const MAX=12,lp=[],hp=[],bp=[],mix=[],sum=ctx.createGain();let prevL=post,prevH=post;for(let i=0;i<MAX;i++){const l=ctx.createBiquadFilter(),h=ctx.createBiquadFilter(),b=ctx.createBiquadFilter(),lm=ctx.createGain(),bm=ctx.createGain(),hm=ctx.createGain();l.type="lowpass";h.type="highpass";b.type="bandpass";l.Q.value=h.Q.value=.707;b.Q.value=3;prevL.connect(l);prevH.connect(h);post.connect(b);l.connect(lm);b.connect(bm);h.connect(hm);lm.connect(sum);bm.connect(sum);hm.connect(sum);prevL=l;prevH=h;lp.push(l);hp.push(h);bp.push(b);mix.push({lm,bm,hm})}sum.connect(wet);wet.connect(lim);lim.connect(output);const params={state:0,cutoff:1000,resonance:.35,poles:4,spread:0,saturation:.5};let envVals=[0,0,0],lfoVals=[0,0,0],last=-1,mods={state:{env:0,lfo:0,envIndex:1,lfoIndex:0},cutoff:{env:0,lfo:0,envIndex:1,lfoIndex:0},resonance:{env:0,lfo:0,envIndex:1,lfoIndex:0},poles:{env:0,lfo:0,envIndex:1,lfoIndex:0},spread:{env:0,lfo:0,envIndex:1,lfoIndex:0},saturation:{env:0,lfo:0,envIndex:1,lfoIndex:0}};function modVal(k){let n;if(k==="cutoff")n=invExpMap(params[k],20,20000);else if(k==="poles")n=(params[k]-1)/11;else n=params[k];const m=mods[k];n=clamp(n+m.env*envVals[m.envIndex]+m.lfo*lfoVals[m.lfoIndex],0,1);if(k==="cutoff")return expMap(n,20,20000);if(k==="poles")return Math.round(1+n*11);return n}function updateAudio(){const now=ctx.currentTime,active=clamp(Math.round(modVal("poles")),1,MAX),spread=clamp(modVal("spread"),0,1),res=clamp(modVal("resonance"),0,1),satur=clamp(modVal("saturation"),0,1),freqs=poleFrequencies(modVal("cutoff"),active,spread),w=stateWeights(modVal("state"));const polePressure=clamp((active-4)/8,0,1),pileup=clamp(polePressure*Math.pow(1-spread,1.7),0,1),wideComb=clamp(polePressure*Math.pow(spread,1.15)*w.bpFocus,0,1),qBase=lerp(.55,32,res)*lerp(1/Math.sqrt(active),1,Math.pow(spread,.65))*lerp(1,.34,pileup)*lerp(1,1.85,wideComb);sum.gain.setTargetAtTime(lerp(1.08,.86,res)*lerp(1,.62,pileup*res)*lerp(1,1.14,wideComb*res),now,.01);for(let i=0;i<MAX;i++){const on=i<active?1:0,f=on?freqs[i]:freqs[0],centered=active===1?0:(i/(active-1))*2-1,q=clamp(qBase*lerp(1.02,.9,Math.abs(centered)*spread),.1,58);[lp[i],hp[i],bp[i]].forEach(n=>{n.frequency.setTargetAtTime(f,now,.008);n.Q.setTargetAtTime(q,now,.008)});const final=on&&i===active-1?1:0,bpGain=lerp(1,.55,pileup*res)*lerp(1,1.42,wideComb*res);mix[i].lm.gain.setTargetAtTime(w.lp*final,now,.008);mix[i].hm.gain.setTargetAtTime(w.hp*final,now,.008);mix[i].bm.gain.setTargetAtTime(w.bp*on/Math.sqrt(active)*lerp(1.5,2.8,res)*bpGain,now,.008)}pre.gain.setTargetAtTime(lerp(.82,4.8,satur),now,.012);post.gain.setTargetAtTime(lerp(1.02,.34,satur),now,.012);wet.gain.setTargetAtTime(lerp(1,.82,satur),now,.012);tone.frequency.setTargetAtTime(lerp(20000,7200,satur),now,.02);if(Math.abs(satur-last)>.01){sat.curve=warmCurve(satur);sat.oversample=satur>.45?"4x":"2x";last=satur}}updateAudio();return{input,output,params,mods,updateAudio,setEnvValues(v){envVals=v.map(x=>clamp(x,0,1));updateAudio()},setLfoValues(v){lfoVals=v.map(x=>clamp(x,-1,1));updateAudio()},destroy(){try{input.disconnect();output.disconnect()}catch(e){}}}}
const defaults={state:0,cutoffNorm:invExpMap(1000,20,20000),resonance:.35,poles:4,spread:0,saturation:.5},ui={...defaults},modUi={state:.5,cutoffNorm:.5,resonance:.5,poles:.5,spread:.5,saturation:.5},envSel={state:1,cutoffNorm:1,resonance:1,poles:1,spread:1,saturation:1},lfoSel={state:0,cutoffNorm:0,resonance:0,poles:0,spread:0,saturation:0};
const params=[{key:"state",label:"STATE",min:0,max:1,step:.001,display:()=>stateLabel(ui.state),modKey:"state"},{key:"cutoffNorm",label:"CUTOFF",min:0,max:1,step:.001,display:()=>`${Math.round(getCutoff())} Hz`,modKey:"cutoff"},{key:"resonance",label:"RESONANCE",min:0,max:1,step:.001,display:()=>`${Math.round(ui.resonance*100)}%`,modKey:"resonance"},{key:"poles",label:"POLES",min:1,max:12,step:1,display:()=>`${ui.poles} / ${ui.poles*6}dB`,modKey:"poles"},{key:"spread",label:"SPREAD",min:0,max:1,step:.001,display:()=>`${Math.round(ui.spread*100)}%`,modKey:"spread"},{key:"saturation",label:"SATURATION",min:0,max:1,step:.001,display:()=>`${Math.round(ui.saturation*100)}%`,modKey:"saturation"}];
function stateLabel(v){if(v<.08)return"LPF";if(v>.92)return"HPF";if(v<.44)return"LP→BP";if(v<.57)return"BPF";return"BP→HP"}function getCutoff(){return expMap(ui.cutoffNorm,20,20000)}
function effectiveNorm(key){let n;if(key==="cutoffNorm")n=ui.cutoffNorm;else if(key==="poles")n=(ui.poles-1)/11;else n=ui[key];const m=modUi[key],envAmt=m<.5?(.5-m)*2:0,lfoAmt=m>.5?(m-.5)*2:0;return clamp(n+envAmt*liveMod.env[envSel[key]]+lfoAmt*liveMod.lfo[lfoSel[key]],0,1)}
function effectiveParams(){return{state:effectiveNorm("state"),cutoff:expMap(effectiveNorm("cutoffNorm"),20,20000),resonance:effectiveNorm("resonance"),poles:Math.round(1+effectiveNorm("poles")*11),spread:effectiveNorm("spread"),saturation:effectiveNorm("saturation")}}
const knobEls=new Map();function createKnobs(){const root=document.getElementById("knobs");root.innerHTML="";params.forEach(def=>{const stack=document.createElement("div");stack.className="knob-stack";stack.innerHTML=`<div class="knob-label">${def.label}</div>`;const k=makeKnob(false),r=document.createElement("div");r.className="readout";const ml=document.createElement("div");ml.className="mod-label";ml.textContent="ENV ◀ MOD ▶ LFO";const row=document.createElement("div");row.className="mod-row";const env=document.createElement("select");env.className="mini-select";env.innerHTML='<option value="1">E2</option><option value="2">E3</option>';env.value=envSel[def.key];const mk=makeKnob(true);const lfo=document.createElement("select");lfo.className="mini-select lfo";lfo.innerHTML='<option value="0">L1</option><option value="1">L2</option><option value="2">L3</option>';lfo.value=lfoSel[def.key];row.append(env,mk.el,lfo);const mr=document.createElement("div");mr.className="readout mod-readout";stack.append(k.el,r,ml,row,mr);root.appendChild(stack);knobEls.set(def.key,{def,face:k.face,readout:r});knobEls.set("mod_"+def.key,{def:{...def,min:0,max:1,step:.001},face:mk.face,readout:mr});env.addEventListener("change",()=>{envSel[def.key]=parseInt(env.value);applyUiToFilter();drawLcd()});lfo.addEventListener("change",()=>{lfoSel[def.key]=parseInt(lfo.value);applyUiToFilter();drawLcd()});attachKnob(def,k.el,false);attachKnob({...def,key:def.key,min:0,max:1,step:.001},mk.el,true);updateKnobVisual(def.key);updateModVisual(def.key)})}function makeKnob(small){const el=document.createElement("div");el.className="knob"+(small?" small":"");const face=document.createElement("div");face.className="knob-face";face.innerHTML='<div class="indicator"></div>';el.appendChild(face);return{el,face}}
function attachKnob(def,el,isMod){let dragging=false,sx=0,sy=0,sv=0;const range=def.max-def.min;function setVal(next){if(def.step>=1)next=Math.round(next);else next=Math.round(next/def.step)*def.step;if(isMod){modUi[def.key]=clamp(next,0,1);updateModVisual(def.key)}else{ui[def.key]=clamp(next,def.min,def.max);updateKnobVisual(def.key)}applyUiToFilter();drawLcd()}el.addEventListener("pointerdown",e=>{dragging=true;sx=e.clientX;sy=e.clientY;sv=isMod?modUi[def.key]:ui[def.key];el.setPointerCapture(e.pointerId);e.preventDefault()});el.addEventListener("pointermove",e=>{if(!dragging)return;setVal(sv+((sy-e.clientY)+(e.clientX-sx)*.35)*range/260*(e.shiftKey?.18:1));e.preventDefault()});el.addEventListener("pointerup",e=>{dragging=false;try{el.releasePointerCapture(e.pointerId)}catch(x){}});el.addEventListener("dblclick",()=>{if(isMod){modUi[def.key]=.5;updateModVisual(def.key);applyUiToFilter();drawLcd()}});el.addEventListener("wheel",e=>{e.preventDefault();setVal((isMod?modUi[def.key]:ui[def.key])-Math.sign(e.deltaY)*range/120*(e.shiftKey?.2:1))},{passive:false})}
function updateKnobVisual(key){const it=knobEls.get(key),def=it.def,p=(ui[key]-def.min)/(def.max-def.min),deg=-135+p*270;it.face.style.transform=`rotate(${deg}deg)`;it.readout.textContent=def.display()}function updateModVisual(key){const it=knobEls.get("mod_"+key),v=modUi[key],deg=-135+v*270;it.face.style.transform=`rotate(${deg}deg)`;it.readout.textContent=v<.48?`ENV ${Math.round((.5-v)*200)}%`:v>.52?`LFO ${Math.round((v-.5)*200)}%`:"OFF"}
const ampEnv={a:.01,d:.12,s:.75,r:.28},filterEnv={a:.02,d:.18,s:.55,r:.35},modEnv={a:.05,d:.25,s:.45,r:.55};
function createAdsrPanel(id,obj){const root=document.getElementById(id);[["a","ATTACK",0,2],["d","DECAY",0,2],["s","SUSTAIN",0,1],["r","RELEASE",0,3]].forEach(([k,l,min,max])=>{const div=document.createElement("div");div.className="field";div.innerHTML=`<label>${l}</label><input type="range" min="${min}" max="${max}" step="0.001" value="${obj[k]}"><span class="value"></span>`;const inp=div.querySelector("input"),val=div.querySelector("span");function upd(){obj[k]=parseFloat(inp.value);val.textContent=k==="s"?Math.round(obj[k]*100)+"%":obj[k].toFixed(2)+"s"}inp.addEventListener("input",upd);upd();root.appendChild(div)})}
const envState=[{value:0,gate:false,stage:"idle",start:0,startValue:0},{value:0,gate:false,stage:"idle",start:0,startValue:0},{value:0,gate:false,stage:"idle",start:0,startValue:0}];
function envConfig(i){return i===0?ampEnv:i===1?filterEnv:modEnv}
function envGateOn(i){const e=envState[i];e.gate=true;e.stage="attack";e.start=performance.now();e.startValue=e.value}
function envGateOff(i){const e=envState[i];e.gate=false;e.stage="release";e.start=performance.now();e.startValue=e.value}
function updateEnvelope(i,now){const e=envState[i],cfg=envConfig(i);let elapsed=(now-e.start)/1000;if(e.stage==="idle"){e.value=0}else if(e.stage==="attack"){const dur=Math.max(.001,cfg.a),t=clamp(elapsed/dur,0,1);e.value=lerp(e.startValue,1,t);if(t>=1){e.stage="decay";e.start=now;e.startValue=1}}else if(e.stage==="decay"){const dur=Math.max(.001,cfg.d),t=clamp((now-e.start)/1000/dur,0,1);e.value=lerp(1,cfg.s,t);if(t>=1){e.stage="sustain";e.value=cfg.s}}else if(e.stage==="sustain"){e.value=cfg.s}else if(e.stage==="release"){const dur=Math.max(.001,cfg.r),t=clamp(elapsed/dur,0,1);e.value=lerp(e.startValue,0,t);if(t>=1){e.stage="idle";e.value=0}}liveMod.env[i]=clamp(e.value,0,1)}
const lfos=[{wave:"sine",freqNorm:invExpMap(2,.1,200),phase:0,sh:0,shHold:0},{wave:"triangle",freqNorm:invExpMap(.7,.1,200),phase:0,sh:0,shHold:0},{wave:"sample & hold",freqNorm:invExpMap(4,.1,200),phase:0,sh:0,shHold:0}];function lfoFreq(i){return expMap(lfos[i].freqNorm,.1,200)}function resetLfos(){lfos.forEach(l=>{l.phase=0;l.sh=Math.random()*2-1;l.shHold=0});liveMod.lfo=[0,0,0]}
function createLfoPanels(){const root=document.getElementById("lfoPanels");lfos.forEach((l,i)=>{const box=document.createElement("div");box.className="lfo-box";box.innerHTML=`<div class="section-title">LFO${i+1}</div><div class="adsr-grid"><div class="field"><label>WAVEFORM</label><select><option>sine</option><option>triangle</option><option>saw</option><option>square</option><option>sample & hold</option></select><span class="value"></span></div><div class="field"><label>FREQ</label><input type="range" min="0" max="1" step="0.001"><span class="value"></span></div></div>`;const sel=box.querySelector("select"),inp=box.querySelector("input"),vals=box.querySelectorAll(".value");sel.value=l.wave;inp.value=l.freqNorm;function upd(){l.wave=sel.value;l.freqNorm=parseFloat(inp.value);vals[0].textContent=l.wave;vals[1].textContent=lfoFreq(i).toFixed(lfoFreq(i)<10?2:1)+" Hz"}sel.addEventListener("change",upd);inp.addEventListener("input",upd);upd();root.appendChild(box)})}
const audio={ctx:null,filter:null,master:null,analyser:null,saw:null,notes:new Map(),lastT:performance.now(),lfoGate:false,lfoStopTimer:null};async function ensureAudio(){if(audio.ctx)return;const C=window.AudioContext||window.webkitAudioContext,ctx=new C();await ctx.resume();audio.ctx=ctx;audio.saw=makePeriodicSaw(ctx);audio.filter=createPolypoleFilter(ctx);audio.analyser=ctx.createAnalyser();audio.analyser.fftSize=4096;audio.analyser.smoothingTimeConstant=.55;audio.analyser.minDecibels=-110;audio.analyser.maxDecibels=-10;audio.master=ctx.createGain();audio.master.gain.value=.55;audio.filter.output.connect(audio.analyser);audio.filter.output.connect(audio.master);audio.master.connect(ctx.destination);applyUiToFilter();document.getElementById("led").classList.add("on")}
function applyUiToFilter(){if(!audio.filter)return;Object.assign(audio.filter.params,{state:ui.state,cutoff:getCutoff(),resonance:ui.resonance,poles:ui.poles,spread:ui.spread,saturation:ui.saturation});for(const def of params){const v=modUi[def.key],env=v<.5?(.5-v)*2:0,lfoA=v>.5?(v-.5)*2:0;audio.filter.mods[def.modKey]={env,lfo:lfoA,envIndex:envSel[def.key],lfoIndex:lfoSel[def.key]}}audio.filter.updateAudio()}
function startGatedLfos(){clearTimeout(audio.lfoStopTimer);if(!audio.lfoGate){resetLfos();audio.lastT=performance.now()}audio.lfoGate=true}function scheduleLfoStopAfterAmpRelease(){clearTimeout(audio.lfoStopTimer);audio.lfoStopTimer=setTimeout(()=>{if(audio.notes.size===0){audio.lfoGate=false;liveMod.lfo=[0,0,0];if(audio.filter)audio.filter.setLfoValues(liveMod.lfo);drawLcd()}},ampEnv.r*1000+20)}function gateEnvs(on){if(on){envGateOn(0);envGateOn(1);envGateOn(2);startGatedLfos()}else{envGateOff(0);envGateOff(1);envGateOff(2);scheduleLfoStopAfterAmpRelease()}}
function midiToFreq(m){return 440*Math.pow(2,(m-69)/12)}async function noteOn(m){await ensureAudio();if(audio.notes.has(m))return;const ctx=audio.ctx,osc=ctx.createOscillator(),gain=ctx.createGain(),now=ctx.currentTime;osc.setPeriodicWave(audio.saw);osc.frequency.value=midiToFreq(m);gain.gain.cancelScheduledValues(now);gain.gain.setValueAtTime(0,now);gain.gain.linearRampToValueAtTime(.23,now+ampEnv.a);gain.gain.linearRampToValueAtTime(.23*ampEnv.s,now+ampEnv.a+ampEnv.d);osc.connect(gain);gain.connect(audio.filter.input);osc.start();audio.notes.set(m,{osc,gain});setKeyActive(m,true);gateEnvs(true)}function noteOff(m){const n=audio.notes.get(m);if(!n||!audio.ctx)return;const now=audio.ctx.currentTime;n.gain.gain.cancelScheduledValues(now);n.gain.gain.setValueAtTime(n.gain.gain.value,now);n.gain.gain.linearRampToValueAtTime(0,now+ampEnv.r);setTimeout(()=>{try{n.osc.stop();n.osc.disconnect();n.gain.disconnect()}catch(e){}},ampEnv.r*1000+60);audio.notes.delete(m);setKeyActive(m,false);if(audio.notes.size===0)gateEnvs(false)}
function createKeyboard(){const root=document.getElementById("keyboard"),black=[1,3,6,8,10];let wi=0;for(let m=48;m<84;m++){const pc=m%12,isBlack=black.includes(pc),el=document.createElement("div");el.dataset.midi=m;el.className=isBlack?"black-key":"white-key";if(isBlack){el.style.left=((wi-1)*42+28)+"px"}else{el.style.left=(wi*42)+"px";wi++}el.textContent=noteName(m);el.addEventListener("pointerdown",e=>{noteOn(m);el.setPointerCapture(e.pointerId)});el.addEventListener("pointerup",()=>noteOff(m));el.addEventListener("pointerleave",e=>{if(e.buttons)noteOff(m)});root.appendChild(el)}}function noteName(m){return["C","C#","D","D#","E","F","F#","G","G#","A","A#","B"][m%12]+(Math.floor(m/12)-1)}function setKeyActive(m,on){const el=document.querySelector(`[data-midi="${m}"]`);if(el)el.classList.toggle("active",on)}
const keyMap={z:48,s:49,x:50,d:51,c:52,v:53,g:54,b:55,h:56,n:57,j:58,m:59,q:60,"2":61,w:62,"3":63,e:64,r:65,"5":66,t:67,"6":68,y:69,"7":70,u:71,i:72,"9":73,o:74,"0":75,p:76,"[":77,"=":78,"]":79},held=new Set();window.addEventListener("keydown",e=>{const k=e.key.toLowerCase();if(keyMap[k]!==undefined&&!held.has(k)){held.add(k);noteOn(keyMap[k]);e.preventDefault()}});window.addEventListener("keyup",e=>{const k=e.key.toLowerCase();if(keyMap[k]!==undefined){held.delete(k);noteOff(keyMap[k]);e.preventDefault()}});
function drawLcd(){const c=document.getElementById("lcd"),g=c.getContext("2d"),w=c.width,h=c.height;g.clearRect(0,0,w,h);const grad=g.createLinearGradient(0,0,0,h);grad.addColorStop(0,"#06131f");grad.addColorStop(1,"#020617");g.fillStyle=grad;g.fillRect(0,0,w,h);g.strokeStyle="rgba(103,232,249,.13)";for(let i=0;i<=6;i++){const y=i/6*h;g.beginPath();g.moveTo(0,y);g.lineTo(w,y);g.stroke()}const xForF=f=>Math.log10(f/20)/Math.log10(20000/20)*w;[20,50,100,200,500,1000,2000,5000,10000,20000].forEach(f=>{const x=xForF(f);g.beginPath();g.moveTo(x,0);g.lineTo(x,h);g.stroke()});const ep=effectiveParams(),active=ep.poles,cutoff=ep.cutoff,poles=poleFrequencies(cutoff,active,ep.spread),wts=stateWeights(ep.state);function responseAt(f){const res=ep.resonance,polePressure=clamp((active-4)/8,0,1),pileup=clamp(polePressure*Math.pow(1-ep.spread,1.7),0,1),wideComb=clamp(polePressure*Math.pow(ep.spread,1.15)*wts.bpFocus,0,1),visualRes=res*lerp(1,.48,pileup)*lerp(1,1.55,wideComb);let lpDb=0,hpDb=0,bpDb=-30;for(const fc of poles){const r=f/fc,peak=visualRes*8*Math.exp(-Math.pow(Math.log2(r),2)/.035),d=Math.abs(Math.log2(r));lpDb+=-10*Math.log10(1+r*r)+peak;hpDb+=-10*Math.log10(1+1/(r*r))+peak;bpDb+=visualRes*30*Math.exp(-(d*d)/.018)-visualRes*5*Math.exp(-(d*d)/.12)}for(let i=0;i<active-1;i++){const mid=Math.sqrt(poles[i]*poles[i+1]),d=Math.abs(Math.log2(f/mid));bpDb-=lerp(6,22,visualRes)*Math.exp(-(d*d)/.035)}return clamp(20*Math.log10(Math.pow(10,lpDb/20)*wts.lp+Math.pow(10,bpDb/20)*wts.bp+Math.pow(10,hpDb/20)*wts.hp+1e-6),-72,24)}g.beginPath();for(let x=0;x<w;x++){const f=20*Math.pow(20000/20,x/w),db=responseAt(f),y=h-((db+72)/96)*h;if(x===0)g.moveTo(x,y);else g.lineTo(x,y)}g.strokeStyle="rgba(34,211,238,.98)";g.lineWidth=3;g.shadowColor="rgba(34,211,238,.7)";g.shadowBlur=12;g.stroke();g.shadowBlur=0;poles.forEach((fc,i)=>{const x=xForF(fc);g.strokeStyle=`rgba(250,204,21,${.35+.55*(i+1)/poles.length})`;g.beginPath();g.moveTo(x,h*.08);g.lineTo(x,h*.92);g.stroke();g.fillStyle="rgba(250,204,21,.9)";g.beginPath();g.arc(x,h*.16,3,0,Math.PI*2);g.fill()});g.fillStyle="rgba(207,250,254,.85)";g.font="11px ui-monospace,monospace";g.fillText("LIVE MODULATED POLYPOLE RESPONSE",12,20);g.fillText(`${active} poles / ${active*6} dB-oct`,12,36);g.fillText(`${Math.round(cutoff)} Hz`,w-82,20);g.fillStyle="rgba(251,191,36,.88)";g.fillText(`E2 ${liveMod.env[1].toFixed(2)} E3 ${liveMod.env[2].toFixed(2)}  LFO gate ${audio.lfoGate?"ON":"OFF"}  L1 ${liveMod.lfo[0].toFixed(2)} L2 ${liveMod.lfo[1].toFixed(2)} L3 ${liveMod.lfo[2].toFixed(2)}`,12,54)}
const spectrum={peaks:new Array(300).fill(-120),peakTimes:new Array(300).fill(0)};function drawSpectrum(){const c=document.getElementById("spectrum"),g=c.getContext("2d"),W=300,H=300,minF=20,maxF=20000;g.fillStyle="#020617";g.fillRect(0,0,W,H);g.strokeStyle="rgba(148,163,184,.16)";for(let i=0;i<=6;i++){const y=i*H/6;g.beginPath();g.moveTo(0,y);g.lineTo(W,y);g.stroke()}const xForF=f=>Math.log10(f/minF)/Math.log10(maxF/minF)*W;[20,50,100,200,500,1000,2000,5000,10000,20000].forEach(f=>{const x=xForF(f);g.beginPath();g.moveTo(x,0);g.lineTo(x,H);g.stroke()});if(audio.analyser){const a=audio.analyser,data=new Float32Array(a.frequencyBinCount);a.getFloatFrequencyData(data);const sr=a.context.sampleRate,now=performance.now(),yForDb=db=>clamp(H-((db+110)/100)*H,0,H);g.beginPath();for(let x=0;x<W;x++){const f=minF*Math.pow(maxF/minF,x/W),idx=clamp(Math.round(f/(sr/2)*(data.length-1)),0,data.length-1),db=Number.isFinite(data[idx])?data[idx]:-120;if(db>spectrum.peaks[x]||now-spectrum.peakTimes[x]>100){spectrum.peaks[x]=db;spectrum.peakTimes[x]=now}const y=yForDb(db);if(x===0)g.moveTo(x,y);else g.lineTo(x,y)}g.strokeStyle="rgba(45,212,191,.95)";g.lineWidth=2;g.stroke();g.fillStyle="rgba(251,191,36,.9)";for(let x=0;x<W;x+=2)g.fillRect(x,yForDb(spectrum.peaks[x]),1.5,2)}g.fillStyle="rgba(226,232,240,.82)";g.font="10px ui-monospace,monospace";g.fillText("SPECTRUM ANALYZER",8,16);g.fillText("20 Hz",8,H-8);g.fillText("20 kHz",W-48,H-8);requestAnimationFrame(drawSpectrum)}
function lfoValueAt(i,dt){const l=lfos[i],f=lfoFreq(i);l.phase=(l.phase+dt*f)%1;const p=l.phase;if(l.wave==="sine")return Math.sin(p*Math.PI*2);if(l.wave==="triangle")return 1-4*Math.abs(Math.round(p-.25)-(p-.25));if(l.wave==="saw")return p*2-1;if(l.wave==="square")return p<.5?1:-1;if(performance.now()>l.shHold){l.sh=Math.random()*2-1;l.shHold=performance.now()+1000/f}return l.sh}
function modLoop(){const now=performance.now(),dt=(now-audio.lastT)/1000;audio.lastT=now;for(let i=0;i<3;i++)updateEnvelope(i,now);if(audio.lfoGate){for(let i=0;i<3;i++)liveMod.lfo[i]=lfoValueAt(i,dt)}else{liveMod.lfo=[0,0,0]}if(audio.filter){audio.filter.setEnvValues(liveMod.env);audio.filter.setLfoValues(liveMod.lfo)}drawLcd();requestAnimationFrame(modLoop)}
createKnobs();createAdsrPanel("filterAdsr",filterEnv);createAdsrPanel("modAdsr",modEnv);createAdsrPanel("ampAdsr",ampEnv);createLfoPanels();createKeyboard();drawLcd();drawSpectrum();modLoop();window.addEventListener("beforeunload",()=>{try{if(audio.ctx)audio.ctx.close()}catch(e){}});
</script>
</body>
</html>